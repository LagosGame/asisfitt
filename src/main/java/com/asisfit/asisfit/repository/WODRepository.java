package com.asisfit.asisfit.repository;

import com.asisfit.asisfit.model.WOD;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WODRepository extends JpaRepository<WOD,Long> {
}